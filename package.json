{
  "name": "indexdb-helper",
  "version": "0.0.1",
  "description": "Can log \"hello world\" and \"goodbye world\" to the console!",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "dev": "ts-node-dev src/index.ts",
    "dev2": "tsc",
    "build": "tsc --build",
    "start": "NODE_ENV=production node -r dotenv/config dist/index.js",
    "format": "prettier --ignore-path .gitignore --write \"**/*.+(ts|json|css|html|tsx)\"",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "fake-indexeddb": "^5.0.2",
    "jest": "^29.7.0",
    "prettier": "^3.2.4",
    "ts-jest": "^29.1.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
